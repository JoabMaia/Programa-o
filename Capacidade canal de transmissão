#include<stdio.h>
#include<string.h>

int main(){

    double tmv, a, d, cc, QDmax;
    scanf("%lf %lf %lf %lf", &tmv, &a, &d, &cc);

    QDmax = (tmv*5.2 + a*3.4 + d*1.5) / cc;

    printf("%.2lf\n", QDmax);

    return 0;
}
