#include<stdio.h>
#include<string.h>

int main(){

    int num1, num2,i;
    scanf("%d %d", &num1, &num2);

    for(i=num1; i<num2+1; i++)
        if(i%2!=0)
            printf("%d\n", i);

    return 0;

}
