#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include<math.h>

int main(){

    double cateto1, cateto2, hipotenusa;

    scanf("%lf %lf", &cateto1, &cateto2);

    hipotenusa = pow(cateto1, 2) + pow(cateto2, 2);

    printf("%.2lf\n", sqrt(hipotenusa));

    return 0;
}
