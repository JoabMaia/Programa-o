#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int main(){

    int n, m, cont=0, i;

    scanf("%d %d", &n, &m);

    for(i=n; i<m+1; i++){
        if(i%5 == 0){
           if(cont == 0){
            printf("%d", i);
            cont += 1;
           }
            else{
             printf("|%d", i);
            }
    }
    }
    
    return 0;
}
